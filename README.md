# JavaScript Logic & Loops: The 50-Task Challenge

This repository contains a comprehensive set of **50 JavaScript programming exercises** focused on loops, nested structures, mathematical logic, and string manipulation. These tasks progress from basic sequence generation to complex number theory and pattern design.

---

## 🚀 Overview

The primary goal of this project is to master **control flow** and **algorithmic thinking** in JavaScript. Every task is implemented using core language features without relying on external libraries.

### Core Concepts Covered:
* **Looping Structures:** `for`, `while`, and `do-while` loops.
* **Nested Logic:** Multi-dimensional iteration for patterns and multiplication tables.
* **Digit Manipulation:** Extracting, summing, and reversing digits using string methods and mathematical operators.
* **Math Theory:** Prime number detection, Fibonacci sequences, Factorials, and Armstrong numbers.

---

## 📂 Project Sections

### 1. Fundamentals (Tasks 1–4, 13–15)
Covers basic iterations, including custom string joining, sum of ranges, and identifying even/odd sequences.

### 2. Geometric Patterns (Tasks 5–7, 12, 17–20)
Advanced use of **nested loops** to generate:
* Alpha-numeric grids.
* Right-angled and inverted triangles.
* **Floyd's Triangle** and diagonal matrices.

### 3. Practical Logic (Tasks 8–11, 16)
Classic programming challenges:
* **FizzBuzz** implementation.
* **Factorial** calculation via loops.
* **Fibonacci** sequence generation.
* Multiplication tables (1–5).

### 4. Advanced Digit Analysis (Tasks 21–50)
The "Heavy Lifters" — these tasks require deep logic to analyze numbers within specific ranges (e.g., 1–1000):
* **Reverse Logic:** Comparing numbers to their reversed versions.
* **Digit Property Checks:** Finding numbers where the digit sum equals the digit product.
* **Order Analysis:** Identifying numbers with ascending or descending digits.
* **Prime Reversals:** Numbers whose reverse form is a prime number.

---

# 🧵 JavaScript — String Manipulation Exercises

> **30 exercises** to master JavaScript string methods, from basics to advanced challenges.

---

## 🟢 Beginner (1 – 10)

1. Concatenation
2. String Length
3. Access First Character
4. Extract Part of a String
5. Replace Characters
6. Convert to Uppercase
7. Split a String
8. Join Array into String
9. Remove Extra Spaces
10. Check if a Word Exists

---

## 🟡 Intermediate (11 – 20)

11. Reverse a String
12. Count Character Occurrences
13. Palindrome Check
14. Capitalize Each Word
15. Format Number
16. Extract Numbers from String
17. Email Validation
18. Replace Word
19. Remove Duplicate Characters
20. Sort Characters Alphabetically

---

## 🔴 Advanced (21 – 30)

21. Check for Anagrams
22. Reverse Words in Sentence
23. Find the Longest Word
24. Advanced Palindrome
25. Character Frequency
26. Replace Vowels with Numbers
27. `startsWith`
28. `endsWith`
29. `repeat`
30. `indexOf`

---

# 🚀 Advanced JavaScript — Array & Function Challenges

> **30 challenges** using JavaScript functions, array methods, and loops.

---

## 📋 Rules

- ✅ Every solution must be written as a **function**
- ❌ Do **NOT** use `map()`
- 🔁 Combine **Array methods + loops**
- ❌ Do **NOT** solve tasks using a **loop only**
- ✅ Use **array methods whenever possible**
- 🧪 Test your functions with **multiple examples**

---

## 🟢 Level 1 — Array Manipulation (1 – 5)

1. `firstAndLast(arr)`
2. `removeMiddle(arr)`
3. `swapFirstLast(arr)`
4. `insertAtMiddle(arr, value)`
5. `duplicateArray(arr)`

---

## 🟡 Level 2 — Filtering & Searching (6 – 10)

6. `removeDuplicates(arr)`
7. `findSecondLargest(arr)`
8. `findSecondSmallest(arr)`
9. `removeGreaterThan(arr, value)`
10. `removeLessThan(arr, value)`

---

## 🔵 Level 3 — Advanced Array Logic (11 – 15)

11. `countOccurrences(arr, value)`
12. `arrayIntersection(arr1, arr2)`
13. `arrayDifference(arr1, arr2)`
14. `uniqueMerge(arr1, arr2)`
15. `removeNegativeNumbers(arr)`

---

## 🔴 Level 4 — String & Array Combined (16 – 20)

16. `wordsLongerThan(arr, length)`
17. `countLetterInWords(arr, letter)`
18. `removeShortWords(arr, length)`
19. `findLongestWord(arr)`
20. `findShortestWord(arr)`

---

## 🟣 Level 5 — Complex Challenges (21 – 25)

21. `sortByLength(arr)`
22. `removeEverySecond(arr)`
23. `sumEvenNumbers(arr)`
24. `sumOddIndex(arr)`
25. `reverseWithoutReverse(arr)`

---

## ⚡ Level 6 — Hard Challenges (26 – 30)

26. `findMissingNumber(arr)`
27. `pairSum(arr, target)`
28. `rotateArray(arr, steps)`
29. `chunkArray(arr, size)`
30. `groupByLength(arr)`

---

---

# 🔄 JavaScript — `map()` & `forEach()` Practice

> **20 exercises** to practice array iteration using `.map()` and `.forEach()`.

---

## 📋 Rules

- ✅ Use `.map()` when the task **returns a new array**
- ✅ Use `.forEach()` when the task **doesn't need to return** (e.g., counting, summing)
- 🧪 Test each function with the provided test cases

---

## 🟢 Beginner (1 – 10)

1. `doubleNumbers(arr)` — Double each number
2. `numbersToStrings(arr)` — Convert numbers to strings
3. `upperCaseNames(arr)` — Uppercase all names
4. `addFive(arr)` — Add 5 to each number
5. `getNames(arr)` — Extract names from objects
6. `wordLengths(arr)` — Return length of each word
7. `valTimesIndex(arr)` — Multiply each value by its index
8. `makeUsernames(arr)` — Add `@` prefix to usernames
9. `squareNumbers(arr)` — Return squares of numbers
10. `firstLetters(arr)` — Get first letter of each word

---

## 🟡 Intermediate (11 – 20)

11. `sumNumbers(arr)` — Total sum using `forEach`
12. `countEven(arr)` — Count even numbers
13. `timesTen(arr)` — Multiply each number by 10
14. `addAdult(arr)` — Add `adult` boolean property to objects
15. `addTax(arr)` — Add 10% tax to each price
16. `countBig(arr)` — Count numbers greater than 50
17. `labelNumbers(arr)` — Convert to `"Number: X"` format
18. `productNames(arr)` — Extract product names from objects
19. `addIndex(arr)` — Add index to each number
20. `isEvenArray(arr)` — Return boolean array (true if even)

---

---

# 🎮 JavaScript Scope & Output Challenges — Interactive Quiz

> **15 interview-level questions** to test deep understanding of `var` vs `let` behavior.

---

## 🎯 Objectives

- Test deep understanding of **scope**
- Identify common **interview traps**
- Master **hoisting**, **closures**, and **async behavior**

---

## 🧩 Topics Covered

| Tag | Concept |
|-----|---------|
| 🟣 Hoisting | Variable & function hoisting traps |
| 🟠 TDZ | Temporal Dead Zone with `let` |
| 🔵 Scope | Function scope vs block scope |
| 🟢 Closure | Nested functions & variable capture |
| 🩷 Async | `setTimeout` + loop behavior |

---

## 📝 Questions Overview

| # | Title | Concept |
|---|-------|---------|
| 1 | Hoisting Trap | `var` before declaration |
| 2 | Temporal Dead Zone | `let` before declaration |
| 3 | Function vs Block Scope | `var` inside `if` block |
| 4 | Shadowing + TDZ Trap | `let` in function with TDZ |
| 5 | Classic Closure Problem | `var` + `setTimeout` in loop |
| 6 | Closure with `let` | `let` + `setTimeout` in loop |
| 7 | Nested Scope | Closure over outer variable |
| 8 | Re-declaration Error | `var` then `let` same name |
| 9 | Function vs Variable Hoisting | Function declaration priority |
| 10 | Scope + Mutation | Modifying outer `let` from inside |
| 11 | Block Scope Leakage | `var` inside `{}` block |
| 12 | Block Scope (`let`) | `let` inside `{}` block |
| 13 | Parameter Shadowing | Param vs outer variable |
| 14 | Closure + Async Trap | `var` loop + `setTimeout` |
| 15 | IIFE Fix | IIFE as closure solution |

---

## 🚀 How to Play

1. Open `js-quiz.html` in your browser
   link: https://h-othman1515.github.io/Portfolio/js-quiz.html
2. Read the code snippet carefully
3. Pick the correct output from 4 options
4. Read the explanation to understand **why**
5. Track your score at the end 🏆

---

*Happy Coding! 💻✨*
